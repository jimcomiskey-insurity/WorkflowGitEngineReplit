# Environment Variables for Local Development

# E2E Test URLs
# These tell the E2E tests where to find your running application
# Default: localhost (for local development)
# Override these when testing against a deployed environment
TEST_FRONTEND_URL=http://localhost:4200
TEST_BACKEND_URL=http://localhost:5000

# Backend API Configuration (optional)
# Uncomment to customize the backend port
# ASPNETCORE_URLS=http://localhost:5000

# Frontend Configuration (optional)
# Uncomment to customize the frontend port  
# FRONTEND_PORT=4200

# How to Use:
# 1. Copy this file to .env (not tracked by git)
# 2. Update values as needed for your environment
# 3. Source the file before running tests:
#
#    PowerShell:
#      Get-Content .env | ForEach-Object {
#        if ($_ -match '^([^=]+)=(.*)$') {
#          [Environment]::SetEnvironmentVariable($matches[1], $matches[2])
#        }
#      }
#
#    Command Prompt:
#      for /f "tokens=*" %i in (.env) do set %i
#
#    Git Bash:
#      export $(cat .env | xargs)
